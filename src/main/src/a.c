/*
 * every c program has a main function
 * it is called when the program executes
 * #include is a preprocessor directive that tells the compiler to put code from a file into the source code before creating the executable
 */
#include<stdio.h>
int main(int argc,char** argv){
	printf("Hello world\n");
	return 0;
}
